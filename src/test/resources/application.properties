topic.order-change-event=updates.order
topic.lineitem-change-event=updates.line-item
topic.aggregate-order=order.aggregated

quarkus.kafka.devservices.enabled=false

kafka-streams.replication.factor=1
quarkus.kafka-streams.application-id=test
quarkus.kafka-streams.commit.interval.ms=1000
kafka-streams.auto.offset.reset=earliest

kafka-streams.state.dir=target/data/kafka-data/stores

quarkus.log.console.level=INFO
